package java_basic3;

public class TreeSet {
	// TreeSet : 이진검색트리(binary search tree)라는 자료구조의 형태로 데이터를 저장하는 컬렉션.
	// 			  정렬, 검색, 범위검색(range search)에 높은 성능을 보이는 자류구조.
	// 			 TreeSet는 이진 검색 트리의 성능을 향상시킨 '레드-블랙 트리(Read-black tree)'로 구현.
	// 			 Set이기 때문에 중복된 데이터의 저장 허용 하지 않고 정렬된 위치에 저장하므로 저장순서를 유지 하지 않음.
	
	// 이진 검색 트리
	// 1. 모든 노드는 최대 두 개의 자식노드를 가질 수 있음.
	// 2. 왼쪽 자식노드의 값은 부모노드이 값보다 작고 오른쪽 자식노드의 값은 부모노드의 값보다 커야 함.
	// 3. 노드의 추가 삭제에 시간이 걸림(순차적 저장하지 않으므로).
	// 4. 검색(범위검색)과 정렬에 유리.
	// 5. 중복된 값을 저장하지 못함.
}
